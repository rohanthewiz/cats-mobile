// GENERATED BY cmd/catgen-dart — DO NOT EDIT.
//
// The cell attribute bitmask carried in `cell.m`.
//
// Source of truth (in the cats repo):
//   internal/orchestration/protocol.go (the ratatui Modifier bits)
//
// Regenerate:  go run ./cmd/catgen-dart -out <this directory>
//
// A stale copy fails cats's own `make check` — cmd/catgen-dart holds a
// committed golden and diffs it, so adding a field to a wire struct without
// regenerating breaks the GO build, not just this package's.
//
// No == / hashCode is generated. Deep value equality over the List and Map
// fields would need package:collection, and this package is deliberately
// zero-dependency; round-trip tests compare jsonEncode(toJson()) instead.

// dart format off

// These constants are UNEXPORTED in Go — the reason this generator parses
// the source rather than reflecting over it. Retyping six bit patterns by
// hand is exactly the kind of transcription the renderer cannot afford to
// get wrong: a misplaced bit is not a crash, it is italics where there
// should be underline, forever.

/// Attribute bits for `cell.m`. Test with `cell.m & CellAttr.bold != 0`.
///
/// Reversed is a SWAP of fg and bg at paint time, not a colour of its own —
/// resolve it after the frame defaults have been applied, or a reversed cell
/// with omitted colours swaps two zeros and disappears.
abstract final class CellAttr {
  static const int bold = 0x1; // 0b0000_0000_0001
  static const int dim = 0x2; // 0b0000_0000_0010
  static const int italic = 0x4; // 0b0000_0000_0100
  static const int underlined = 0x8; // 0b0000_0000_1000
  static const int reversed = 0x40; // 0b0000_0100_0000
  static const int crossedOut = 0x100; // 0b0001_0000_0000
}
